# Test configuration
spring.profiles.active=test

# Disable Kafka completely for tests
# kafka.bootstrap-servers=
# This property is intentionally commented out to disable @ConditionalOnProperty

# Disable MongoDB for tests  
# spring.data.mongodb.uri=

# Logging configuration
logging.level.org.apache.kafka=ERROR
logging.level.org.springframework.kafka=ERROR
logging.level.org.springframework.data.mongodb=ERROR
logging.level.reactor.kafka=ERROR
logging.level.com.tiger.pocs.kafka=ERROR
logging.level.root=WARN